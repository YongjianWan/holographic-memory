# SESSION legacy status snapshot — 2026-06-24

> 这是从 `SESSION.md` 移出的旧状态块，归档自 checkpoint commit
> `4f9aef0 chore: checkpoint holographic workspace state`。
>
> **不要把本文件当当前状态源。** 当前状态以仓库根目录
> [SESSION.md](../../SESSION.md) 为准。本文件只保留当时的判断语境，避免信息丢失。

# Holo 当前状态与下一步（快过期 — 随时改,以此为准）

> **保质期:天天可能变。** 状态/进度/排序以本份为最新。
> **冲突规则**:本份与 ①宪法、②方案规格冲突时——**仅限状态/进度/排序**以本份为准;**准则**(红线/为什么/规矩)永远以 ①宪法为准,本份无权改准则。
> 来源:《进度总结》《和chatgpt聊天》结论、原《覆盖关系小抄》、原交接稿 §4(已过期,并入此处更新)。
> 最后更新:基于 1051 active 快照口径。**改这份时记得更新这行。**

---

## 一句话状态

底盘做出来了,不是快收尾了——接下来进入"把真实库洗干净并产品化"的阶段。代码冲到 ★3 门口,但真实数据清洗一步没闭环。

## 真实进度（按完成度,1051 active 口径）

| 模块                               |              完成度 | 说明                                                                          |
| ---------------------------------- | ------------------: | ----------------------------------------------------------------------------- |
| A 线检索(FTS/Jaccard/RRF/中文优化) |                 85% | 剩 scope/filter 层                                                            |
| P0 探重                            |             95-100% | 基本 done                                                                     |
| P1-1 entity 归一化                 |             90-100% | 版本后缀误并风险在 TECH_DEBT                                                  |
| 数据清洗                           |              25-35% | 已 1690 soft-deleted,Doc 6/8/9 重抽过;人眼复核+干净库未闭环                   |
| Gate A/B 验证                      |              35-45% | 框架+ledger 有;单值 scope 已弃;库变 1051 active 后**必须重跑**          |
| scope schema                       |                  5% | 只有方向无实现,gated on Gate B                                                |
| P1-2 语义合并+trust/recency        |              40-60% | consolidation/soft delete/recency v8/GC busy 有实现+测试;生产策略未放开       |
| P1-4 跨话题串联                    |              10-20% | 只有规格+边界,gated on Gate A                                                 |
| **P2 建边**                  | **veto/冻结** | 真实数据 fan-out 不支持,不按未完成算                                          |
| 并发补丁                           |              25-35% | BEGIN IMMEDIATE/GC busy skip/PASSIVE/事务原子化有;完整多 agent 方案未系统落地 |
| 抽独立包                           |                  0% | —                                                                            |

**口径**:按"研究型插件+当前 Hermes 可用"算 ~65%;按"完整产品化(独立包+并发+clean corpus+scope+P1-4)"算 40-45%。

## 当前唯一重要的事

**代码主干冲到 ★3 门口,但真实数据清洗零闭环。** ★3 所有阈值(尤其 HRR 语义合并阈值,0.97 不能直接搬)都得在真实数据上标,空库上调=拍脑袋。数据清洗不是 ★3 之后的事,是 ★3 的前置。

## 四个悬而未决(答案在数据里,代码读不出)

1. **翻库 Gate A go/no-go** — 肉眼过 50 条,"去掉人这条还成立吗"。全是"Aiden 干了啥"→图谱层一行别写。
2. **四个待标阈值** — 现在全是文档**起点值**不是**终点值**(Jaccard 0.8、HRR 语义合并阈值、tanh conf、trust 删除阈值)。
3. **token 成本** — 没实测。
4. **提炼粒度** — 没验,试跑时盯 SNR warning。

---

## 下一步排序（取代《改造计划》§8 的 A/B 两线顺序）

1. **基于 1051 active 快照重跑 Gate A/B**(先把批次 ledger 补上,否则口径还是混)
2. **人工复核 dirty/meta 候选**,确认当前库干净度
3. **补数据账本**:extraction_runs → fact_provenance(去 confidence) → quality_reason+dirty_candidate(详见 ②方案规格)
4. **再决定 scope**:多对多 / document-provenance / 继续冻结(取决于 Gate B)
5. **并发 + 独立包**放最后,不然包装的是还在变形的内核

> 数据填充姿势(绝不梭哈,因删不掉库):批量脚本 → 先试 3-5 个核粒度+token 成本 → 粒度 OK 才按时间正序跑全量 → 导完立刻翻 50 条 go/no-go。

---

## 文档覆盖关系（哪份推翻哪份）

| 旧           | 哪部分过时/被推翻                 | 以谁为准                                      |
| ------------ | --------------------------------- | --------------------------------------------- |
| 改造计划 §5 | GC 触发="hermes 进程内定时器"     | ②方案规格 并发方案:"任一活着的实例+抢锁跳过" |
| 改造计划 §6 | P2="待实现砍成 shared-entity 边"  | 本份:P2=**veto/冻结**(数据推翻)         |
| 改造计划 §8 | 落地顺序 A/B 两线                 | 本份"下一步排序"                              |
| 交接稿 §4   | "数据填充零""retain 没喂真实文件" | 本份进度表(1051/1690)                         |

**原始《改造计划》33KB 大文档已降级为 archive** — 不删(铁律:不可再生的别删),但不再进"施工时要读"列表。它的 §5/§6/§8 被推翻,但"为什么推翻"的语境留在里面。

---

## 一个待补设计点(ChatGPT 评估的唯一净增量)

**有效期语义**:trust 衰减="越老越不信",≠"事实有明确失效时点"。"本周要交付"过了这周就该死,不是慢慢衰减——这俩是不同的东西。记进"提取器 profile"那层,标 `validity/expired 语义待设计`。**不是现在做**——库还没洗干净就讨论有效期语义又是 runs ahead,到了再说。

# Holo 待办与已否决（防止重提清单）

> **这份的用途:防止下一个 Claude(或三个月后的你)重新提一个早就想过并否掉的东西。**
> 每条都带"凭什么不做"和"什么条件才解冻"。看到有人提清单里的事,先翻这份,别从头解释一遍。
> 与 ①宪法准则冲突 → 以①为准。状态/进度 → 以③为准。

---

## A. 已否决 / 已冻结（有结论,别重开)

| 项                                            | 状态                | 凭什么                                                      | 解冻条件                                           |
| --------------------------------------------- | ------------------- | ----------------------------------------------------------- | -------------------------------------------------- |
| **P2 建边(shared-entity)**              | **veto/冻结** | 真实数据 fan-out 0.811、94% entity 只在一条 fact,建边没原料 | 出现"必须靠图才能答"的真实查询                     |
| **P2.5 LLM 有类型边**                   | 默认永不做          | 记忆边主观、会错、无自愈,污染链式查询                       | 同上且 P2 已解冻                                   |
| **单值 scope 列**                       | 判死                | 人的记忆不是规整 taxonomy,一个 fact 跨多域                  | 不解冻(死路);要做 scope 走多对多且 gated on Gate B |
| **cross-encoder rerank**                | 违红线,不做         | 常驻模型。RRF 是官方认可降级                                | 永不(违母红线)                                     |
| **embedding 语义召回**                  | 违红线,不做         | 要常驻 embedding 服务。代价(无语义召回)睁眼认               | 永不(违母红线)                                     |
| **路 B 并发(一 agent 持有库)**          | 不采用              | 持有者变"必须先启动的记忆服务",破无启动序列                 | 永不(违母红线)                                     |
| **reflect loop / consolidation worker** | 不做                | 常驻后台循环                                                | 永不(违母红线);需求已被惰性GC消化                  |

---

## B. Gated — 规格已定,等闸过才动手（不是否决,是没到时候)

| 项                                                               | 卡在哪   | 闸的判据                                                                     |
| ---------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------- |
| **P1-4 跨话题串联**                                        | Gate A   | 翻 50 条 fact,抽得出独立于人的结构 → 才有原料。全是"Aiden 干了啥"→整块作废 |
| **scope 多对多 + category/scope 解耦 + HRR bank 按 scope** | Gate B   | 证明 fact 能稳定分域。证不出 → 多对多和单值一样不做                         |
| **P1-2 生产策略放开跑**                                    | 阈值标定 | 四个待标阈值在真实 1051 库上标完(尤其 HRR 语义合并阈值,0.97 不能搬)          |

> ⚠️ 解 HRR bank 饱和**未必非走 scope**。别把"解饱和"和"上 scope"绑成一个包——也可能 category 内再分桶,或直接认 HRR 弱信号、RRF 里权重低就完事。

---

## C. 推迟 — 想过,判定现在做是 runs ahead（到了再说)

| 项                                      | 来源           | 为什么现在别做                                                                                     | 什么时候轮到                          |
| --------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------- |
| **有效期/validity 语义**          | ChatGPT 净增量 | "本周交付"过期就该死 ≠ trust 慢慢衰减,是两种东西。但库还没洗干净就设计有效期=runs ahead           | 提取器 profile 那层,数据洗完后        |
| **提取器分型(extractor profile)** | ChatGPT 第4类  | 技术文档/会议/聊天各有提取边界。现在一个 prompt 还没调稳,先分型是过早                              | Gate A 过、单一 prompt 验证后         |
| **固定 eval 测试集**              | ChatGPT 第5类  | 20真实查询/20不该召回/20脏数据/20时间线/20溯源。是真问题,但得先有干净库当基准                      | 数据清洗闭环后,改 prompt/retrieval 前 |
| **恢复机制**                      | ChatGPT 第6类  | migration dry-run、rollback、WAL 损坏/半写入 retain 恢复测试。记忆系统底线,但当前单用户低频,未撞到 | 并发/独立包阶段一起做                 |

---

## D. TECH_DEBT — 砍了但记着（过早优化,撞到再说)

| 项                                        | 凭什么砍                                       |
| ----------------------------------------- | ---------------------------------------------- |
| 上万~几十万 fact 全表扫的性能优化         | 个人单用户库,这规模猴年马月,runs ahead         |
| category bank O(N²) 全量重建 → 增量更新 | 审计后再上,现在规模无所谓                      |
| P1-1 版本后缀误并风险(K2/K2.7)            | 已有数字/版本签名闸兜着,真撞到边界 case 再收紧 |
| 自适应预算 / token 截断                   | 没遇到瓶颈                                     |

---

## 一句话

A 别重开,B 等闸别催,C 到点再说,D 撞到再修。**新想法进来先对这张表——重了就停。**

---

*Last updated: 2026-06-24*
